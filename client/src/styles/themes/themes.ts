import { extendTheme } from '@chakra-ui/react';

const colors = {
  backgroundChat: '#1A232E',
  gradientOne: '#CA00FF',
  gradientTwo: '#7AEBFB',
  grayColor: '#999999',
  rgbaWhite: 'rgba(255, 255, 255, 0.1)'
};

export const theme = extendTheme({ colors });
